#include <string.h>
/**
 * _strlen -  for printing length of sting , given the pointer
 * that pints to the first char of the array of char
 * @s: the pointer to the first srray of char
 * Return: returns the lenght of string exludin gthe terminating char
*/
int _strlen(char *s)
{
	return (strlen(s));
}
