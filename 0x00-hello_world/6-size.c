#include <stdio.h>

int main() {
    printf("Size of char: %zu bytes(1)\n", sizeof(char));
    printf("Size of int: %zu bytes(1)\n", sizeof(int));
    printf("Size of float: %zu bytes(1)\n", sizeof(float));
    printf("Size of double: %zu bytes(1)\n", sizeof(double));

    return 0;
}
