#include <ctype.h>
#include <stdio.h>

int main()
{
    // char c;


    // c = 'A';
    // printf("%c: %d\n", c, isupper(c));
    // c = 'a';
    // printf("%c: %d\n", c, isupper(c));

    char x = 'a' ;

    printf("%d\n", isupper(x));

    return (0);

    // 0 in case of lower
    // non zero in case of uuper 
}