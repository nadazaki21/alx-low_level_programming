#include <stdio.h>
#include "main.h"
#include <ctype.h>
/**
* print_alphabet_x10 - function for printing the alphabets
*/

int _islower(int c)
{
    if (islower(c) == 0)
    {
        return (0);
    }

    if (islower(c) != 0)
    {
        return (1);
    }
    

}
